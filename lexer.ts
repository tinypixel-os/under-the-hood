let x = 45;

// [ LetToken, IdentifierToken, EqualsToken, NumberToken, SemicolonToken];

export const TokenType {
    LetToken,
    IdentifierToken,
    EqualsToken,
    NumberToken,
    SemicolonToken,
    WhitespaceToken,

    OpenParenToken,
    CloseParenToken,

    AddToken,
    MinusToken,
    MultiplyToken,
    DivideToken,
}